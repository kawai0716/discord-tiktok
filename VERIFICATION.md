# 検証記録

検証日: 2026-09-23

## 成功した検証

- Python 3.12で、条件判定・通知・履歴・異常系・ブラウザ統合の自動テスト **25件すべて成功**。
- 実Chromiumを使い、外部アクセスをテスト応答に置き換えたブラウザ統合テストを実行。ページ内通信からの動画取得、空一覧の拒否、不完全メタデータの拒否を確認。
- 実ファイルを使った初期化 → 送信失敗 → 保留保存 → 次回成功 → 既知ID保存を確認。
- GitHub保存はモックで初回ブランチ作成順序、SHAによる更新、変更なし保存の省略、読み込みエラーを確認。
- 標準設定と複数ルールのサンプル設定を検証。
- GitHub Actionsの2ファイルをYAMLとして解析し、トリガーとjobsの存在を確認（GitHub上での実行は未検証）。
- 初期化成功を偽装する状態ファイルや、架空の実サイト取得結果は同梱していません。

## 実際のTikTokアクセス

対象: `https://www.tiktok.com/tag/lovely7`

1. 通常HTTPではステータス200、約387KBのHTMLが返りました。
2. HTMLにアプリ初期設定はありましたが、動画データ・動画リンクはありませんでした。
3. Playwright 1.63.0 / Chromiumで実ページを開きました。
4. ページタイトルは `#lovely7 | TikTok` でした。
5. `/api/challenge/item_list/` の応答は **HTTP 200、Content-Typeはapplication/json、本文は0バイト**でした。動画リンクも0件でした。
6. プログラムは取得エラーとして終了しました。dry-runなのでDiscord送信・状態保存はしていません。

**この環境では実動画の取得成功を確認できていません。** アクセス制限等の可能性がありますが、原因は断定できません。現在の取得アダプターは、想定形式のデータが返る環境で動作する実装であり、実サイトでの取得成功を検証済みとするものではありません。

## 未検証

- GitHub Actions上からのTikTok取得。
- GitHubリポジトリへの実際の状態保存。
- 指定Discordチャンネルへの実配送。

リポジトリ・Webhookがまだ設定されていないため、上記は未実行です。READMEの順にdry-runと初期化を実施してください。GitHubでも空応答が続く場合は、その取得環境のまま自動監視を開始できません。

## 再実行

通常のテスト（外部通信なし、ブラウザテスト3件はスキップ）:

```bash
python -m unittest discover -s tests -v
```

ブラウザをインストールした環境で全テスト:

```bash
RUN_BROWSER_TESTS=1 python -m unittest discover -s tests -v
```

PowerShellでは `$env:RUN_BROWSER_TESTS = "1"` を先に実行します。GitHubのTestsワークフローはブラウザテストも含めます。
